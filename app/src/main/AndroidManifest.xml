<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.studio_c">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.CodeActivity" />
        <activity android:name=".activity.WechatActivity" />
        <activity android:name=".activity.CouponActivity" />
        <activity android:name=".activity.CouponDetailActivity" />
        <activity android:name=".activity.Al" />
        <activity android:name=".activity.AlreadyActivity" />
        <activity android:name=".activity.CollectActivity" />
        <activity android:name=".activity.NotificationActivity" />
        <activity android:name=".activity.MessageActivity"></activity>
        <activity android:name=".activity.CouponActivity" />
        <activity android:name=".activity.CouponDetailActivity" />
        <activity android:name=".activity.FeedbackActivity"></activity>
    </application>

</manifest>